name: Use Secret in API Call

on: push

jobs:
  call-api:
    runs-on: ubuntu-latest

    steps:
      - name: Make API request using secret
        run: |
          echo "Calling API with secret..."
          curl -H "Authorization: Bearer ${{ secrets.MY_SECRET }}" https://httpbin.org/bearer
